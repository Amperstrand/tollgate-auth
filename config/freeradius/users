# /etc/freeradius/3.0/users
# Payment validation is handled by the inner-tunnel site config
# via the cashu-auth exec module. See sites-available/inner-tunnel.
# All non-payment users are rejected.
DEFAULT
    Auth-Type := Reject
